{
  "name": "versatiles-fonts",
  "version": "0.0.4",
  "scripts": {
    "generate": "bin/generate.sh",
    "release": "bin/release.sh"
  },
  "dependencies": {
    "@mapbox/glyph-pbf-composite": "^0.0.3",
    "fontnik": "^0.7.0",
    "work-faster": "^0.1.0"
  },
  "engines": {
    "node": ">=16"
  }
}
